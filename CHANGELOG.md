# Changelog

## 0.1.0 (2026-04-16)

Initial public release.

- `AppInterface`, `ModuleInterface`, `ExtensionInterface`
- `ExtensionFactory<T>` for runtime extension instantiation
- `PluginBase` mixin with `assetPrefix` / `asset()` helpers
